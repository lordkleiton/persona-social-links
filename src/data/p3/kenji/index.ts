import { Arcanas } from "../../../enums/arcanas";
import { CharacterData } from "../../../models/character_data";
import links from "./links";

const kenji = new CharacterData(Arcanas.magician, "Kenji", [], [], links);

export default kenji;
